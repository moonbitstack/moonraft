# `.*` would also swallow `.devops/`, which GitLink's pipeline engine reads —
# its absence disables CI with no error, so it is re-included explicitly.
.*
!.git*
!.devops

_build/
_coverage/
_site/
target/

bisect.coverage
cobertura.xml
